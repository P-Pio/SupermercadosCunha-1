import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    body: "Helvetica, sans-serif",  // Use Helvetica for body text
    heading: "Helvetica, sans-serif",  // Use Helvetica for headings
  },
});

export default theme;
